1. git rm <file>
	удалить файл и из репозитория, и из рабочего каталога
2. git rm --cached <file>
	удалить файл из репозитория, но не из рабочего каталога
3. git log
	просмотр изменений в ветке проекта (git log -10, git log --stat, git log --pretty=format"%ca 	%au", git log -S<text>)
4. git mv 
	перемещение файла
5. 